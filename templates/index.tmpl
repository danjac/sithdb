<!DOCTYPE html>
<html>

    <head>
        <title>Obi-Wan's Sith Database</title>
        <link rel="stylesheet" type="text/css" href="{{ .staticURL }}styles.css">
    </head>
    <body>
        <div id="app"></div>
        <script src="{{ .jsBaseURL }}{{ .staticURL }}main.js"></script>
    </body>
</html>
